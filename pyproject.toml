[project]
name = "op"
version = "0.1.0"

[project.scripts]
op = "op.op:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

